<h1>New Enrolment</h1>

<%= form_tag(course_enrolments_path(@course), method: "post") do %>
  <p>
    <%= label_tag(:email, "Email:") %><br>
    <%= text_field_tag(:email) %>
  </p>
 
  <p>
    <%= submit_tag("Create") %>
  </p>
  
<% end %>

<%= link_to 'Back', course_enrolments_path(@course) %>
